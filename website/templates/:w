<!DOCTYPE html>
<html>
    <body>
        {% for doc in results %}
            {% if doc.get('title') %}
                <h4> {{ doc.get('title') }} </h4>
            {% endif %}
            {% if doc.get('contributors') %}
                {% for contributor in doc.get('contributors') %}
                    {{ contributor['full_name'] }}, 
                {% endfor %}
                <br/>
            {% endif %}
            <ul>
            {% for property in doc.get('properties').keys() %}
                <li>{{ property }} : {{ doc.get('properties').get(property) }}</li>
            {% endfor %}
            </ul>
        {% endfor %}
    </body>

</html>
